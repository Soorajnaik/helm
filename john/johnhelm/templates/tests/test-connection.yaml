apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "johnhelm.fullname" . }}-test-connection"
  labels:
    {{- include "johnhelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "johnhelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
